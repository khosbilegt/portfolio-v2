import { Flex } from "@mantine/core";

function Footer() {
  return (
    <Flex
      style={{
        width: "100%",
        height: "100px",
      }}
    >
      <p>Test</p>
    </Flex>
  );
}

export default Footer;
